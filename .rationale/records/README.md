Un Record representa una decisión, restricción, riesgo o conocimiento operativo consolidado (`Rationale_v0.5.md §5.2`), y requiere procedencia, evidencia y — cuando es normativo — aprobación explícita.

`constraint.no-provider-internal-access` es el primer Record real, creado durante Fase D (dogfooding sobre este mismo repositorio, `Proceso §21`). Está en `authority: unreviewed` (sin `approvals`) — no se autoaprueba. Su `binding_declarations` apunta a `src/providers/codebase_memory.rs::CodebaseMemoryClient`, añadido en Fase D3-D4 una vez que ese módulo existió de verdad — no se inventó un binding a código inexistente.
