Un Record representa una decisión, restricción, riesgo o conocimiento operativo consolidado (`Rationale_v0.5.md §5.2`), y requiere procedencia, evidencia y — cuando es normativo — aprobación explícita.

`constraint.no-provider-internal-access` es el primer Record real, creado durante Fase D (dogfooding sobre este mismo repositorio, `Proceso §21`). Está en `authority: unreviewed` (sin `approvals`) — no se autoaprueba. Su `binding_declarations` está vacío a propósito: el módulo `providers` que lo hará cumplir todavía no existe (se crea en Fase D3-D4); el binding se añadirá cuando exista el símbolo real, no antes.
