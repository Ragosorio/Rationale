# Architecture

The conceptual source of truth is still
[`Rationale_Arquitectura_Conceptual_v0.1.md`](../../Rationale_Arquitectura_Conceptual_v0.1.md)
at the repository root. This directory does not reinterpret it; the real
post-pilot Architecture 0.2 (`§23`, Phase I) is still pending.

[`code-map.md`](code-map.md) is different: a **factual** map of the modules that
exist today in `src/` and how the real operations (`prepare`, `serve`,
`finalize_change`, the human authority commands, and `install-agent`) flow end to
end. It describes the code as it is; it neither proposes nor closes new
architectural decisions.
